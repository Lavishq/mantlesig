// const msig = artifacts.require("MultisigFactory");

// contract("MultisigFactory", (accounts) => {
//   it("0 is the first counter", async () => {
//     const msigInstance = await msig.deployed();
//     // below line was causing header not found so i tested using solidity
//     const counter = await msigInstance.getCounter.call(accounts[0]);

//     assert.equal(counter.toValue(), 0, "first count should be 0");
//   });
// });
